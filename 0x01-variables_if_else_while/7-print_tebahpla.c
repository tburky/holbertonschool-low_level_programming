/*
 * file: 7-print_tebahpla.c
 * Auth: Tanner Burkybile <tanner.burkybile@gmail.com>
 */

#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
{
	for (n = 'z' ; n >= 'a' ; n--)
	putchar(n);
}
	putchar (10);

	return (0);
}
